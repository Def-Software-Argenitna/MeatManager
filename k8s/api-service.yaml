apiVersion: v1
kind: Service
metadata:
  name: meatmanager-api
  namespace: meatmanager-demo
spec:
  selector:
    app: meatmanager-api
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  type: ClusterIP
