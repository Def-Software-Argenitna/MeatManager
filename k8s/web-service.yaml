apiVersion: v1
kind: Service
metadata:
  name: meatmanager-web
  namespace: meatmanager-demo
spec:
  selector:
    app: meatmanager-web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP
