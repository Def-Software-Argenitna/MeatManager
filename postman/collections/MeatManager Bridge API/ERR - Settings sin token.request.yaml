$kind: http-request
name: 'ERR - Settings sin token (debe dar 401)'
method: GET
url: '{{baseUrl}}/bridge/settings'
order: 14000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('status 401', () => pm.response.to.have.status(401));
